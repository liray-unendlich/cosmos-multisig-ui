import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert";

interface Props {
  active: boolean;
}

export default function GeneralNews({ active }: Props) {
  // Database migration warning removed - no longer needed
  return null;
}
